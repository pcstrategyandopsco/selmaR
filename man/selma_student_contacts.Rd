% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/contacts.R
\name{selma_student_contacts}
\alias{selma_student_contacts}
\title{Fetch student-contact links from SELMA}
\usage{
selma_student_contacts(
  con = NULL,
  student_id = NULL,
  contact_id = NULL,
  relationship = NULL,
  cache = FALSE,
  cache_dir = "selma_cache",
  cache_hours = 24,
  items_per_page = 100L,
  .progress = TRUE
)
}
\arguments{
\item{con}{A \code{selma_connection} object from \code{\link[=selma_connect]{selma_connect()}}, or \code{NULL}
(default) to use the stored connection.}

\item{student_id}{Filter by student ID.}

\item{contact_id}{Filter by contact ID.}

\item{relationship}{Filter by relationship type.}

\item{cache}{If \code{TRUE}, use RDS caching (default \code{FALSE}).}

\item{cache_dir}{Directory for cache files (default \code{"selma_cache"}).}

\item{cache_hours}{Hours before cache is considered stale (default 24).}

\item{items_per_page}{Items per API page (default 100).}

\item{.progress}{Show progress messages (default \code{TRUE}).}
}
\value{
A tibble with columns including \code{studentid}, \code{contactid},
\code{isemergency}, \code{isprimary}, and \code{relationship}.
}
\description{
Retrieves the relationships between students and their contacts,
including emergency contact and primary contact flags.
}
\examples{
\dontrun{
selma_connect()
student_contacts <- selma_student_contacts()
student_contacts <- selma_student_contacts(student_id = "123")
}
}
