% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auth.R
\name{selma_connect}
\alias{selma_connect}
\title{Connect to the SELMA API}
\usage{
selma_connect(
  base_url = NULL,
  email = NULL,
  password = NULL,
  api_version = NULL,
  config_file = "config.yml"
)
}
\arguments{
\item{base_url}{SELMA base URL (e.g. \code{"https://myorg.selma.co.nz/"}).}

\item{email}{API login email.}

\item{password}{API login password.}

\item{api_version}{SELMA API version: \code{"v2"}, \code{"v3"}, or \code{NULL} (default)
to auto-detect. A SELMA instance runs one version — the value is stored
on the connection object and used for all subsequent requests.}

\item{config_file}{Path to a config YAML file (default \code{"config.yml"}).
Set to \code{NULL} to skip config file lookup.}
}
\value{
A \code{selma_connection} object (invisibly). The connection is also
stored in the package environment for automatic use by all fetch functions.
}
\description{
Authenticates with SELMA and stores the connection for the session.
Once connected, all \verb{selma_*()} fetch functions use this connection
automatically — no need to pass it explicitly.
}
\details{
Credentials are resolved in order:
\enumerate{
\item \strong{Direct arguments} — \code{base_url}, \code{email}, \code{password}
\item \strong{config.yml} — version-specific block (\code{selma.v2} or \code{selma.v3}) if
\code{api_version} is set, then flat \code{selma} block as fallback
\item \strong{Environment variables} — version-specific (\code{SELMA_V3_EMAIL} etc.)
then generic (\code{SELMA_EMAIL} etc.)
}
}
\section{config.yml}{

Create a \code{config.yml} in your project root (add to \code{.gitignore}!).

\strong{Single version} (flat structure, backward-compatible):

\if{html}{\out{<div class="sourceCode yaml">}}\preformatted{default:
  selma:
    base_url: "https://myorg.selma.co.nz/"
    email: "api@selma.co.nz"
    password: "secret"
}\if{html}{\out{</div>}}

\strong{Dual version} (v2 and v3 credentials stored separately):

\if{html}{\out{<div class="sourceCode yaml">}}\preformatted{default:
  selma:
    v2:
      base_url: "https://myorg.selma.co.nz/"
      email: "v2_api@selma.co.nz"
      password: "v2secret"
    v3:
      base_url: "https://myorg.selma.app/"
      email: "v3_api@selma.app"
      password: "v3secret"
}\if{html}{\out{</div>}}

When \code{api_version = "v3"} is set (or auto-detected), selmaR reads
\code{selma.v3.email} / \code{selma.v3.password} first, falling back to the flat
\code{selma.email} / \code{selma.password} if not present.
}

\examples{
\dontrun{
# Connect once — all functions use it automatically (api_version auto-detected)
selma_connect()
students <- selma_students()
enrolments <- selma_enrolments()

# Specify API version explicitly
selma_connect(
  base_url = "https://myorg.selma.co.nz/",
  email = "api@selma.co.nz",
  password = "secret",
  api_version = "v3"
)
}
}
