% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrolments.R
\name{selma_enrolments}
\alias{selma_enrolments}
\title{Fetch enrolment records from SELMA}
\usage{
selma_enrolments(
  con = NULL,
  intake_id = NULL,
  student_id = NULL,
  cache = FALSE,
  cache_dir = "selma_cache",
  cache_hours = 24,
  items_per_page = 100L,
  .progress = TRUE
)
}
\arguments{
\item{con}{A \code{selma_connection} object from \code{\link[=selma_connect]{selma_connect()}}, or \code{NULL}
(default) to use the stored connection.}

\item{intake_id}{Filter by intake ID. v3 only — ignored on v2 with a
warning.}

\item{student_id}{Filter by student ID. v3 only — ignored on v2 with a
warning.}

\item{cache}{If \code{TRUE}, use RDS caching (default \code{FALSE}).}

\item{cache_dir}{Directory for cache files (default \code{"selma_cache"}).}

\item{cache_hours}{Hours before cache is considered stale (default 24).}

\item{items_per_page}{Items per API page (default 100).}

\item{.progress}{Show progress messages (default \code{TRUE}).}
}
\value{
A tibble of enrolment records.
}
\description{
Retrieves all enrolment records linking students to intakes.
}
\examples{
\dontrun{
selma_connect()
enrolments <- selma_enrolments()
enrolments <- selma_enrolments(intake_id = "123")  # v3 only
}
}
