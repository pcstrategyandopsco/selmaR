% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/events.R
\name{selma_events}
\alias{selma_events}
\title{Fetch event records from SELMA (v3)}
\usage{
selma_events(
  con = NULL,
  filter = list(),
  cache = FALSE,
  cache_dir = "selma_cache",
  cache_hours = 24,
  items_per_page = 100L,
  .progress = TRUE
)
}
\arguments{
\item{con}{A \code{selma_connection} object from \code{\link[=selma_connect]{selma_connect()}}, or \code{NULL}
(default) to use the stored connection.}

\item{filter}{Named list of API query parameters, e.g.
\code{list(surname = "Smith", first_name = "Alice")} (v3) or
\code{list(surname = "Smith", forename = "Alice")} (v2).
Unknown names emit a warning and are dropped.}

\item{cache}{If \code{TRUE}, use RDS caching (default \code{FALSE}).}

\item{cache_dir}{Directory for cache files (default \code{"selma_cache"}).}

\item{cache_hours}{Hours before cache is considered stale (default 24).}

\item{items_per_page}{Items per API page (default 100).}

\item{.progress}{Show progress messages (default \code{TRUE}).}
}
\value{
A tibble of event records with columns including \code{id}, \code{student},
\code{enrolment}, \code{intake}, \code{event_type}, \code{event_subject}, \code{event_body},
\code{event_priority}, \code{event_complete}, and \code{event_due_date}.
}
\description{
Retrieves event records from SELMA v3. Events are the v3 equivalent of
notes — they link activity records (comments, tasks, emails) to students,
enrolments, intakes, contacts, or organisations.
}
\details{
In v3, comments (notes) are attached to events rather than directly to
students. To get notes with student context, fetch both and use
\code{\link[=selma_join_notes]{selma_join_notes()}}:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{con <- selma_connect()
notes  <- selma_notes(con)
events <- selma_events(con)
students <- selma_students(con)
notes_with_students <- selma_join_notes(notes, students, events = events)
}\if{html}{\out{</div>}}
}
\examples{
\dontrun{
con <- selma_connect()
events <- selma_events(con)
events <- selma_events(con, filter = list(student = "42"))
}
}
