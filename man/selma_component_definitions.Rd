% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/component_definitions.R
\name{selma_component_definitions}
\alias{selma_component_definitions}
\title{Fetch component definitions from SELMA}
\usage{
selma_component_definitions(
  con = NULL,
  cache = FALSE,
  cache_dir = "selma_cache",
  cache_hours = 24,
  items_per_page = 100L,
  .progress = TRUE
)
}
\arguments{
\item{con}{A \code{selma_connection} object from \code{\link[=selma_connect]{selma_connect()}}, or \code{NULL}
(default) to use the stored connection.}

\item{cache}{If \code{TRUE}, use RDS caching (default \code{FALSE}).}

\item{cache_dir}{Directory for cache files (default \code{"selma_cache"}).}

\item{cache_hours}{Hours before cache is considered stale (default 24).}

\item{items_per_page}{Items per API page (default 100).}

\item{.progress}{Show progress messages (default \code{TRUE}).}
}
\value{
A tibble of component definitions with columns including
\code{compid}, \code{compcode}, \code{comptitle}, \code{compefts}, \code{comptype},
\code{compcrediteq}, and \code{complevelfw}.
}
\description{
Retrieves component (unit standard/course) definitions — the master list
of components available across programmes. This is different from
\code{\link[=selma_components]{selma_components()}} which returns \emph{enrolment} components (instances
of components attached to student enrolments).
}
\examples{
\dontrun{
con <- selma_connect()
comp_defs <- selma_component_definitions(con)
}
}
