% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes.R
\name{selma_student_classes}
\alias{selma_student_classes}
\title{Fetch student class assignments from SELMA}
\usage{
selma_student_classes(
  con = NULL,
  student_id = NULL,
  cache = FALSE,
  cache_dir = "selma_cache",
  cache_hours = 24,
  items_per_page = 100L,
  .progress = TRUE
)
}
\arguments{
\item{con}{A \code{selma_connection} object from \code{\link[=selma_connect]{selma_connect()}}, or \code{NULL}
(default) to use the stored connection.}

\item{student_id}{Filter by student ID.}

\item{cache}{If \code{TRUE}, use RDS caching (default \code{FALSE}).}

\item{cache_dir}{Directory for cache files (default \code{"selma_cache"}).}

\item{cache_hours}{Hours before cache is considered stale (default 24).}

\item{items_per_page}{Items per API page (default 100).}

\item{.progress}{Show progress messages (default \code{TRUE}).}
}
\value{
A tibble of student-class records with columns including
\code{student_id}, \code{class}, \code{campus_id}, \code{start_date}, and \code{end_date}.
}
\description{
Retrieves which students are assigned to which classes.
}
\examples{
\dontrun{
selma_connect()
student_classes <- selma_student_classes()
student_classes <- selma_student_classes(student_id = "123")
}
}
