% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{selma_get}
\alias{selma_get}
\title{Fetch data from a SELMA API endpoint}
\usage{
selma_get(
  con = NULL,
  endpoint,
  query_params = NULL,
  items_per_page = 30L,
  max_pages = Inf,
  .progress = TRUE
)
}
\arguments{
\item{con}{A \code{selma_connection} object from \code{\link[=selma_connect]{selma_connect()}}, or \code{NULL}
to use the connection stored by \code{\link[=selma_connect]{selma_connect()}}.}

\item{endpoint}{API endpoint path (e.g. \code{"students"}, \code{"enrolments"}).}

\item{query_params}{Named list of additional query parameters.}

\item{items_per_page}{Number of items per page (default 30).}

\item{max_pages}{Maximum pages to fetch (default \code{Inf} for all).}

\item{.progress}{Show progress messages via cli (default \code{TRUE}).}
}
\value{
A tibble of results with \code{clean_names()} applied.
}
\description{
Generic paginated fetcher that handles Hydra JSON-LD pagination. For most
use cases, prefer the entity-specific functions like \code{\link[=selma_students]{selma_students()}} or
\code{\link[=selma_enrolments]{selma_enrolments()}}.
}
\examples{
\dontrun{
selma_connect()
students <- selma_get(endpoint = "students", items_per_page = 100)
}
}
