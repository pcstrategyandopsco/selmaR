% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{make_entity_fetcher}
\alias{make_entity_fetcher}
\title{Create a fetch function for any SELMA endpoint}
\usage{
make_entity_fetcher(entity)
}
\arguments{
\item{entity}{API endpoint path segment (v3 canonical name), e.g.
\code{"events"}, \code{"placements"}, \code{"documents"}. Consult the SELMA v3 OpenAPI
spec for the full list. For v2 connections the endpoint is resolved via
the version alias table automatically.}
}
\value{
A function with signature: \cr
\verb{function(con = NULL, filter = list(), cache = FALSE,} \cr
\verb{         cache_dir = "selma_cache", cache_hours = 24,} \cr
\verb{         items_per_page = 100L, .progress = TRUE)}
}
\description{
Returns a function that fetches, paginates, and standardises any SELMA
collection endpoint. Use this to query endpoints not yet covered by a named
function in the package — the SELMA API has over 200 collection endpoints
and selmaR does not wrap all of them.
}
\details{
The returned function validates \code{filter} parameter names against the schema
registry for the connected API version (sourced from the OpenAPI specs at
build time). Unknown parameters emit a \code{cli_warn()} and are dropped rather
than being sent to the API silently.

For a one-off query without creating a named function, use \code{\link[=selma_get]{selma_get()}}
directly.
}
\examples{
\dontrun{
con <- selma_connect()

# Create a reusable function for an endpoint not yet in the package:
selma_events     <- make_entity_fetcher("events")
selma_placements <- make_entity_fetcher("placements")

events     <- selma_events(con)
placements <- selma_placements(con, filter = list(placement_status = "active"))

# For a one-off query, use selma_get() directly:
documents <- selma_get(con, "documents")
visas     <- selma_get(con, "visas")
}
}
