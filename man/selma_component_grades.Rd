% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/component_attempts.R
\name{selma_component_grades}
\alias{selma_component_grades}
\title{Fetch component grade/attempt records from SELMA (v3)}
\usage{
selma_component_grades(
  con = NULL,
  filter = list(),
  cache = FALSE,
  cache_dir = "selma_cache",
  cache_hours = 24,
  items_per_page = 100L,
  .progress = TRUE
)
}
\arguments{
\item{con}{A \code{selma_connection} object from \code{\link[=selma_connect]{selma_connect()}}, or \code{NULL}
(default) to use the stored connection.}

\item{filter}{Named list of API query parameters, e.g.
\code{list(surname = "Smith", first_name = "Alice")} (v3) or
\code{list(surname = "Smith", forename = "Alice")} (v2).
Unknown names emit a warning and are dropped.}

\item{cache}{If \code{TRUE}, use RDS caching (default \code{FALSE}).}

\item{cache_dir}{Directory for cache files (default \code{"selma_cache"}).}

\item{cache_hours}{Hours before cache is considered stale (default 24).}

\item{items_per_page}{Items per API page (default 100).}

\item{.progress}{Show progress messages (default \code{TRUE}).}
}
\value{
A tibble of enrolment component grade records.
}
\description{
Retrieves \code{enrolment_component_grades} records — the v3 equivalent of
\code{\link[=selma_component_attempts]{selma_component_attempts()}}. Each record captures one assessment attempt
against an enrolment component, including the attempt date, note, numerical
value, and grading scheme grade.
}
\details{
The \code{enrolment_component} column contains the IRI-stripped ID of the parent
\code{\link[=selma_components]{selma_components()}} record. Use \code{\link[=selma_join_attempts]{selma_join_attempts()}} to join grades back
to their parent components.
}
\examples{
\dontrun{
con <- selma_connect()
grades <- selma_component_grades(con)
grades <- selma_component_grades(con, filter = list(enrolment_component = "789"))
}
}
