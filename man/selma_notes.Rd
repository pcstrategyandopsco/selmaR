% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/notes.R
\name{selma_notes}
\alias{selma_notes}
\title{Fetch notes and events from SELMA}
\usage{
selma_notes(
  con = NULL,
  cache = FALSE,
  cache_dir = "selma_cache",
  cache_hours = 24,
  items_per_page = 100L,
  .progress = TRUE
)
}
\arguments{
\item{con}{A \code{selma_connection} object from \code{\link[=selma_connect]{selma_connect()}}, or \code{NULL}
(default) to use the stored connection.}

\item{cache}{If \code{TRUE}, use RDS caching (default \code{FALSE}).}

\item{cache_dir}{Directory for cache files (default \code{"selma_cache"}).}

\item{cache_hours}{Hours before cache is considered stale (default 24).}

\item{items_per_page}{Items per API page (default 100).}

\item{.progress}{Show progress messages (default \code{TRUE}).}
}
\value{
A tibble of note/event records with columns including \code{noteid},
\code{student_id}, \code{enrolmentid}, \code{notetype}, \code{notearea}, \code{note1}, and
\code{confidential}.
}
\description{
Retrieves notes and event records linked to students and enrolments.
Notes include pastoral care records, meeting notes, and other
student-related documentation.
}
\examples{
\dontrun{
con <- selma_connect()
notes <- selma_notes(con)

# Notes for a specific student
student_notes <- notes |>
  dplyr::filter(student_id == "123")
}
}
