% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/documents.R
\name{selma_upload_document}
\alias{selma_upload_document}
\title{Upload a document to SELMA (v3 only)}
\usage{
selma_upload_document(
  con = NULL,
  file,
  document_type,
  student = NULL,
  event = NULL
)
}
\arguments{
\item{con}{A \code{selma_connection} object from \code{\link[=selma_connect]{selma_connect()}}, or \code{NULL}
to use the stored connection.}

\item{file}{Path to a local file to upload.}

\item{document_type}{Document type, as an ID or IRI. Required.}

\item{student}{Optional student to associate, as an ID or IRI.}

\item{event}{Optional event to associate, as an ID or IRI.}
}
\value{
A single-row tibble describing the created document, with
\code{clean_names()} applied.
}
\description{
Uploads a local file and creates a Document resource in SELMA, optionally
associating it with a student, event, or intake enrolment. Wraps the
\code{POST /api/documents} multipart endpoint.
}
\details{
This endpoint exists only in the SELMA v3 API. Calling it on a v2 connection
errors — document upload is not available in v2.

\code{document_type}, \code{student}, and \code{event} accept either a bare ID (e.g. \code{1} or
\code{"1"}) or a full IRI (\code{"/api/document_types/1"}); bare IDs are expanded to
the IRI form the API expects. Look up valid document types with
\code{selma_get(con, "document_types")}.
}
\examples{
\dontrun{
con <- selma_connect()

# See which document types are configured in your instance
selma_get(con, "document_types")

# Upload a signed enrolment agreement against a student
selma_upload_document(
  con,
  file          = "agreements/student-42-signed.pdf",
  document_type = 1,
  student       = 42
)
}
}
