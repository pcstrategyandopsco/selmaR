% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/organisations.R
\name{selma_organisations}
\alias{selma_organisations}
\title{Fetch organisations from SELMA}
\usage{
selma_organisations(
  con = NULL,
  org_id = NULL,
  name = NULL,
  third_party_id = NULL,
  registration_number = NULL,
  cache = FALSE,
  cache_dir = "selma_cache",
  cache_hours = 24,
  items_per_page = 100L,
  .progress = TRUE
)
}
\arguments{
\item{con}{A \code{selma_connection} object from \code{\link[=selma_connect]{selma_connect()}}, or \code{NULL}
(default) to use the stored connection.}

\item{org_id}{Filter by organisation ID.}

\item{name}{Filter by organisation name (exact match).}

\item{third_party_id}{Filter by third-party ID.}

\item{registration_number}{Filter by registration number.}

\item{cache}{If \code{TRUE}, use RDS caching (default \code{FALSE}).}

\item{cache_dir}{Directory for cache files (default \code{"selma_cache"}).}

\item{cache_hours}{Hours before cache is considered stale (default 24).}

\item{items_per_page}{Items per API page (default 100).}

\item{.progress}{Show progress messages (default \code{TRUE}).}
}
\value{
A tibble of organisation records with columns including \code{id},
\code{name}, \code{legalname}, \code{orgtype}, \code{email}, \code{phone}, and \code{country}.
}
\description{
Retrieves organisation records (employers, agents, partner institutions).
}
\examples{
\dontrun{
selma_connect()
orgs <- selma_organisations()
orgs <- selma_organisations(name = "Acme Ltd")
}
}
