% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes.R
\name{selma_classes}
\alias{selma_classes}
\title{Fetch class definitions from SELMA}
\usage{
selma_classes(
  con = NULL,
  class_name = NULL,
  campus_id = NULL,
  enrstatus = NULL,
  cache = FALSE,
  cache_dir = "selma_cache",
  cache_hours = 24,
  items_per_page = 100L,
  .progress = TRUE
)
}
\arguments{
\item{con}{A \code{selma_connection} object from \code{\link[=selma_connect]{selma_connect()}}, or \code{NULL}
(default) to use the stored connection.}

\item{class_name}{Filter by class name (exact match).}

\item{campus_id}{Filter by campus ID.}

\item{enrstatus}{Filter by enrolment status.}

\item{cache}{If \code{TRUE}, use RDS caching (default \code{FALSE}).}

\item{cache_dir}{Directory for cache files (default \code{"selma_cache"}).}

\item{cache_hours}{Hours before cache is considered stale (default 24).}

\item{items_per_page}{Items per API page (default 100).}

\item{.progress}{Show progress messages (default \code{TRUE}).}
}
\value{
A tibble of class records with columns including \code{id},
\code{class_name}, \code{capacity}, \code{startdate}, \code{enddate}, \code{campusid},
and \code{enrolment_count}.
}
\description{
Retrieves timetable class records with capacity and date information.
}
\examples{
\dontrun{
selma_connect()
classes <- selma_classes()
classes <- selma_classes(campus_id = "1")
}
}
