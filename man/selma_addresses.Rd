% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/addresses.R
\name{selma_addresses}
\alias{selma_addresses}
\title{Fetch addresses from SELMA}
\usage{
selma_addresses(
  con = NULL,
  student_id = NULL,
  contact_id = NULL,
  org_id = NULL,
  cache = FALSE,
  cache_dir = "selma_cache",
  cache_hours = 24,
  items_per_page = 100L,
  .progress = TRUE
)
}
\arguments{
\item{con}{A \code{selma_connection} object from \code{\link[=selma_connect]{selma_connect()}}, or \code{NULL}
(default) to use the stored connection.}

\item{student_id}{Filter by student ID.}

\item{contact_id}{Filter by contact ID.}

\item{org_id}{Filter by organisation ID.}

\item{cache}{If \code{TRUE}, use RDS caching (default \code{FALSE}).}

\item{cache_dir}{Directory for cache files (default \code{"selma_cache"}).}

\item{cache_hours}{Hours before cache is considered stale (default 24).}

\item{items_per_page}{Items per API page (default 100).}

\item{.progress}{Show progress messages (default \code{TRUE}).}
}
\value{
A tibble of address records with columns including \code{addressid},
\code{studentid}, \code{contactid}, \code{orgid}, \code{street}, \code{suburb}, \code{city},
\code{region}, \code{postcode}, and \code{country}.
}
\description{
Retrieves address records linked to students, contacts, or organisations.
}
\examples{
\dontrun{
selma_connect()
addresses <- selma_addresses()
addresses <- selma_addresses(student_id = "123")
}
}
