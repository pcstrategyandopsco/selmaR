% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/programmes.R
\name{selma_programmes}
\alias{selma_programmes}
\title{Fetch programme definitions from SELMA}
\usage{
selma_programmes(
  con = NULL,
  status = NULL,
  cache = FALSE,
  cache_dir = "selma_cache",
  cache_hours = 24,
  items_per_page = 100L,
  .progress = TRUE
)
}
\arguments{
\item{con}{A \code{selma_connection} object from \code{\link[=selma_connect]{selma_connect()}}, or \code{NULL}
(default) to use the stored connection.}

\item{status}{Filter by programme status (e.g. \code{"Active"}, \code{"Inactive"}).
v2 only (\code{progstatus}) — ignored on v3 with a warning.}

\item{cache}{If \code{TRUE}, use RDS caching (default \code{FALSE}).}

\item{cache_dir}{Directory for cache files (default \code{"selma_cache"}).}

\item{cache_hours}{Hours before cache is considered stale (default 24).}

\item{items_per_page}{Items per API page (default 100).}

\item{.progress}{Show progress messages (default \code{TRUE}).}
}
\value{
A tibble of programme records.
}
\description{
Retrieves programme (qualification) records.
}
\examples{
\dontrun{
selma_connect()
programmes <- selma_programmes()
programmes <- selma_programmes(status = "Active")
}
}
