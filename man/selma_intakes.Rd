% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/intakes.R
\name{selma_intakes}
\alias{selma_intakes}
\title{Fetch intake definitions from SELMA}
\usage{
selma_intakes(
  con = NULL,
  prog_id = NULL,
  status = NULL,
  start_before = NULL,
  start_after = NULL,
  cache = FALSE,
  cache_dir = "selma_cache",
  cache_hours = 24,
  items_per_page = 100L,
  .progress = TRUE
)
}
\arguments{
\item{con}{A \code{selma_connection} object from \code{\link[=selma_connect]{selma_connect()}}, or \code{NULL}
(default) to use the stored connection.}

\item{prog_id}{Filter by programme ID.}

\item{status}{Filter by intake status (e.g. \code{"Open"}, \code{"Closed"}).}

\item{start_before}{Filter intakes starting before this date (ISO string).}

\item{start_after}{Filter intakes starting after this date (ISO string).}

\item{cache}{If \code{TRUE}, use RDS caching (default \code{FALSE}).}

\item{cache_dir}{Directory for cache files (default \code{"selma_cache"}).}

\item{cache_hours}{Hours before cache is considered stale (default 24).}

\item{items_per_page}{Items per API page (default 100).}

\item{.progress}{Show progress messages (default \code{TRUE}).}
}
\value{
A tibble of intake records.
}
\description{
Retrieves intake (cohort) records with dates and programme links.
}
\examples{
\dontrun{
selma_connect()
intakes <- selma_intakes()
intakes <- selma_intakes(prog_id = "42")
intakes <- selma_intakes(start_after = "2025-01-01")
}
}
