# Funding source code to label mapping

Named character vector mapping funding source codes to human-readable
labels.

## Usage

``` r
SELMA_FUNDING_LABELS
```

## Format

An object of class `character` of length 5.
